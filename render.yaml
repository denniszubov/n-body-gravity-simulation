services:
  - type: web
    name: n-body-gravity-simulation
    runtime: python
    plan: free
    buildCommand: pip install ".[app]"
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.10
